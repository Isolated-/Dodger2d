{
  "name": "dodger",
  "version": "0.1.0-alpha.0",
  "description": "A super basic 2d game written in pure JavaScript.",
  "main": "dist/dodger.js",
  "license": "MIT",
  "author": "Michael Palmer",
  "scripts": {
    "dev": "watchify init.js -o dist/dodger.js -s Dodger -v",
    "build": "browserify init.js -o dist/dodger.js -s Dodger > dist/dodger.js",
    "test": "jest ./test"
  },
  "devDependencies": {
    "@babel/plugin-proposal-class-properties": "^7.8.3",
    "browserify": "^16.5.0",
    "jest": "^25.1.0",
    "watchify": "^3.11.1"
  },
  "repository": "git@github.com:Isolated-/Dodger2d.git"
}
